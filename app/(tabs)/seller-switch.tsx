import { View } from 'react-native';

// This is a placeholder screen that won't be rendered
// The actual switching is handled in the tab layout
export default function SellerSwitchScreen() {
  return <View />;
}
